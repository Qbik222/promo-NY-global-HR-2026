# Аналіз верстки проєкту Favbet Promo Template

## 📋 Загальна структура проєкту

### Архітектура
Проєкт використовує класичну архітектуру з розділенням на `front/` (вихідні файли) та `build/` (згенеровані файли). Це типовий підхід для Gulp-based проєктів.

**Структура:**
- `front/` - вихідні файли (Pug, SASS, JS)
- `build/` - згенеровані файли (HTML, CSS, JS)
- Gulp як build tool

---

## 🔧 Build System (Gulp)

### Підходи та особливості:

1. **Режими збірки:**
   - Development: з sourcemaps, без мініфікації
   - Production: з мініфікацією (cleanCSS, uglify)
   - Визначення режиму через `process.argv[3] === '--prod'`

2. **Завдання Gulp:**
   - `sass` - компіляція SASS → CSS з autoprefixer
   - `pug` - компіляція Pug → HTML
   - `js` - транспіляція через Babel + конкатенація
   - `copyImg`, `copyFonts`, `copyJson` - копіювання статичних файлів
   - `clean` - очищення build директорії
   - `watch` + `server` - dev режим з BrowserSync

3. **Особливості:**
   - Використання `gulp-newer` для оптимізації (копіювання тільки нових файлів)
   - `gulp-plumber` + `gulp-notify` для обробки помилок
   - Sourcemaps тільки в development режимі

### Проблеми та рекомендації:
- ❌ В `watch` завданні є помилка: `copyFonts` викликається для `front/img/**`, має бути `front/fonts/**`
- ✅ Добре використано `gulp-if` для умовної обробки
- ⚠️ Відсутня оптимізація зображень через `gulp-imagemin` (хоча пакет встановлений)

---

## 📝 Template Engine (Pug)

### Синтаксис та стиль:

1. **Структура:**
   - Використання `extends` для наслідування шаблонів
   - Блоки (`block head`, `block content`) для модульності
   - Включення через `include`

2. **Особливості верстки:**
   - Використання BEM методології (`.block__element_modifier`)
   - Семантичні HTML5 теги (`<picture>`, `<details>`, `<summary>`)
   - Responsive images через `<picture>` з `source` тегами
   - Атрибути `data-translate` для інтернаціоналізації

3. **Приклади коду:**
```pug
.welcome__left
    picture.welcome__title
        source(media="(max-width: 600px)" srcset='...')
        img(src="..." alt="Favbet")
```

### Проблеми:
- ⚠️ Використання абсолютних URL для зображень (`https://fav-prom.com/...`) замість відносних шляхів
- ✅ Добре структурований код з коментарями для закоментованих елементів

---

## 🎨 Стилізація (SASS)

### Методологія та підходи:

1. **Організація файлів:**
   - Модульна структура: `var.sass`, `def.sass`, `fonts.sass`, `main.sass`
   - Окремі файли для сторінок: `pages/index.sass`, `pages/eng.sass`, `pages/dark.sass`
   - Окремий файл медіа-запитів: `media.sass`

2. **Синтаксис SASS:**
   - Використання **indented syntax** (`.sass` замість `.scss`)
   - Відсутність фігурних дужок та крапок з комою
   - Вкладеність через відступи

3. **BEM методологія:**
```sass
.welcome
    &__left
        display: flex
    &__title
        max-width: 604px
```

4. **Змінні:**
   - Централізовані в `var.sass`
   - Кольори, шрифти, розміри
   - Використання `$` префіксу

5. **Responsive Design:**
   - Mobile-first підхід (медіа-запити в окремому файлі)
   - Breakpoints: 1368px, 900px, 760px, 600px, 376px, 360px
   - Спеціальні медіа-запити для landscape орієнтації

6. **Анімації:**
   - Використання `@keyframes` для складних анімацій
   - CSS animations замість JavaScript
   - Приклади: `levitate`, `rotate-clockwise`, `ball-rotate-levitate`

### Особливості стилізації:

- **Flexbox та Grid:**
  - Flexbox для більшості layout'ів
  - CSS Grid для `.participate__steps` (3 колонки)

- **Позиціювання:**
  - Використання `position: absolute` для декоративних елементів
  - `calc()` для точних розрахунків позицій

- **Кастомізація scrollbar:**
```sass
&::-webkit-scrollbar-thumb
    background: $green
```

### Проблеми та рекомендації:

- ⚠️ Використання абсолютних URL для зображень в CSS (`url("https://fav-prom.com/...")`)
- ✅ Добре організована структура змінних
- ⚠️ Дублювання медіа-запитів (в `index.sass` та `media.sass`)
- ✅ Використання сучасних CSS властивостей (`backdrop-filter`, `will-change`)

---

## 💻 JavaScript

### Підходи та патерни:

1. **Структура:**
   - IIFE (Immediately Invoked Function Expression) для ізоляції scope
   - Асинхронні функції (`async/await`)
   - Fetch API для HTTP запитів

2. **Функціональність:**
   - **Інтернаціоналізація (i18n):**
     - Завантаження перекладів з API
     - Заміна тексту через `data-translate` атрибути
     - Підтримка локалей (en, hr)

   - **Infinite Scroll Animation:**
     - Динамічне створення елементів для безкінечного скролу
     - Використання `requestAnimationFrame` для плавної анімації
     - Обчислення ширини елементів для правильного позиціювання

   - **Dropdown функціональність:**
     - Використання нативних `<details>` елементів
     - Кастомна обробка `toggle` події для scroll behavior

   - **Loader:**
     - Спиннер під час завантаження
     - Приховування після ініціалізації

3. **Обробка помилок:**
   - Try-catch блоки
   - Відправка помилок на сервер (`reportError`)
   - Редирект на stub сторінку при помилках

4. **Визначення користувача:**
   - Очікування `window.store` або `window.g_user_id`
   - Polling з інтервалом 50ms (максимум 20 спроб)

### Стиль коду:

- ✅ Використання `const` та `let` (ES6+)
- ✅ Arrow functions
- ✅ Template literals
- ⚠️ Закоментований код (слід видалити або використати TODO коментарі)
- ⚠️ Магічні числа (50, 20, 300) - краще винести в константи

### Проблеми:

- ⚠️ Hardcoded URL: `https://fav-prom.com/api_black_prize`
- ⚠️ Відсутність error boundaries для критичних операцій
- ✅ Добре використано `requestAnimationFrame` для анімацій

---

## 🎯 Code Style та Конвенції

### Позитивні практики:

1. **BEM методологія:**
   - Чітке іменування: `.block__element_modifier`
   - Приклад: `.welcome__left`, `.promo__element_ball`

2. **Організація коду:**
   - Модульна структура файлів
   - Логічне групування функцій
   - Коментарі для важливих секцій

3. **Семантичний HTML:**
   - Використання нативних HTML5 елементів
   - Правильні атрибути (`alt`, `aria-*`)

### Проблеми:

1. **Абсолютні URL:**
   - Зображення та API endpoints використовують абсолютні URL
   - Ускладнює локальну розробку та тестування

2. **Закоментований код:**
   - Багато закоментованих блоків в Pug та JS
   - Слід видалити або використати систему версіонування

3. **Магічні значення:**
   - Числа без пояснень (2600px, 120px, тощо)
   - Краще винести в константи/змінні

4. **Inconsistent naming:**
   - Змішування стилів (camelCase в JS, kebab-case в CSS)

---

## 📱 Responsive Design

### Підхід:

1. **Breakpoints:**
   - Desktop: > 1368px
   - Tablet: 600px - 1368px
   - Mobile: < 600px
   - Додаткові: 900px, 760px, 376px, 360px

2. **Техніки:**
   - Responsive images через `<picture>`
   - Flexbox/Grid для адаптивних layout'ів
   - `transform: scale()` для пропорційного масштабування

3. **Landscape орієнтація:**
   - Спеціальні медіа-запити для landscape режиму
   - Приклад: `@media screen and (max-width: 950px) and (max-height: 450px)`

### Проблеми:

- ⚠️ Багато breakpoints може ускладнити підтримку
- ✅ Добре продумана адаптація для різних пристроїв

---

## 🔍 SEO та Accessibility

### Позитивні практики:

- ✅ Семантичні HTML теги
- ✅ Атрибути `alt` для зображень
- ✅ Правильна структура заголовків

### Проблеми:

- ⚠️ Відсутність `lang` атрибута для динамічної зміни мови
- ⚠️ Відсутність ARIA атрибутів для інтерактивних елементів
- ⚠️ Відсутність мета-тегів для SEO

---

## 🚀 Рекомендації для покращення

### Пріоритетні:

1. **Замінити абсолютні URL на відносні:**
   ```sass
   // Замість:
   background: url("https://fav-prom.com/...")
   // Використати:
   background: url("../img/...")
   ```

2. **Видалити закоментований код:**
   - Або використати Git для зберігання історії
   - Або додати TODO коментарі з поясненням

3. **Винести константи:**
   ```javascript
   const SCROLL_WIDTH = 2600;
   const SCROLL_SPEED = 0.3;
   const MAX_ATTEMPTS = 20;
   ```

4. **Додати environment variables:**
   ```javascript
   const API_URL = process.env.API_URL || 'https://fav-prom.com/api_black_prize';
   ```

5. **Виправити помилку в gulpfile.js:**
   ```javascript
   // Рядок 114:
   gulp.watch('front/fonts/**/*.*', gulp.series('copyFonts'));
   ```

### Додаткові покращення:

1. **Додати TypeScript** для типізації JavaScript
2. **Використати CSS Custom Properties** для динамічних значень
3. **Додати ESLint правила** для консистентності коду
4. **Оптимізувати зображення** через gulp-imagemin
5. **Додати unit tests** для критичної функціональності

---

## 📊 Висновок

### Сильні сторони:

✅ Модульна структура проєкту  
✅ Використання сучасних технологій (SASS, Pug, ES6+)  
✅ BEM методологія для CSS  
✅ Добре продуманий responsive design  
✅ Використання нативних HTML5 елементів  
✅ Плавні CSS анімації  

### Слабкі сторони:

❌ Абсолютні URL замість відносних  
❌ Закоментований код  
❌ Магічні значення  
❌ Помилка в gulpfile.js (watch для fonts)  
❌ Відсутність environment variables  

### Загальна оцінка:

Проєкт демонструє **добре структурований код** з використанням сучасних підходів до верстки. Основні проблеми пов'язані з конфігурацією та організацією, а не з архітектурою. З невеликими покращеннями проєкт може стати production-ready.

**Оцінка: 7.5/10**

