# Аналіз репозиторію promo-global-hr

## 📋 Загальна інформація

**Назва проєкту:** Promo Global HR  
**Тип:** Landing page для промо-акції Favbet  
**Build tool:** Gulp 4  
**Template engine:** Pug  
**CSS preprocessor:** SASS (indented syntax)  
**JavaScript:** ES6+ з Babel transpilation

---

## 🏗️ Структура проєкту

```
promo-global-hr/
├── front/              # Вихідні файли
│   ├── js/            # JavaScript файли
│   ├── sass/          # SASS стилі
│   ├── pug/           # Pug шаблони
│   ├── img/           # Зображення
│   └── fonts/         # Шрифти
├── build/             # Згенеровані файли (production)
├── node_modules/      # Залежності
├── gulpfile.js        # Gulp конфігурація
├── package.json       # Залежності та скрипти
└── readme.md          # Документація
```

---

## 🔧 Технологічний стек

### Build System
- **Gulp 4** - task runner
- **Babel** - транспіляція ES6+ → ES5
- **BrowserSync** - dev server з live reload
- **Sourcemaps** - тільки в development режимі

### Frontend
- **Pug** - шаблонізатор HTML
- **SASS** (indented syntax) - CSS препроцесор
- **Vanilla JavaScript** (ES6+) - без фреймворків
- **Canvas API** - для анімацій (олень, сніжинки)

### Стилізація
- **BEM методологія** - для іменування класів
- **Flexbox/Grid** - для layout'ів
- **CSS Animations** - для плавних анімацій
- **Responsive Design** - mobile-first підхід

---

## 📝 Основна функціональність

### 1. Інтернаціоналізація (i18n)
- Завантаження перекладів з API (`/new-translates/${locale}`)
- Підтримка локалей: `hr` (хорватська), `en` (англійська)
- Динамічна заміна тексту через `data-translate` атрибути
- Автоматичне визначення локалі через hidden input елементи

### 2. Canvas анімації
- **Олень (Deer Animation):**
  - Спрайт-лист з 12 кадрами (6 кадрів × 2 напрямки)
  - Стани: ходьба, пасіння, idle
  - Автоматичний рух між межами з пасінням на краях
  
- **Сніжинки (Snowflakes):**
  - 50 сніжинок з випадковими параметрами
  - Плавна анімація падіння з вітром
  - Адаптивний розмір canvas під контент

### 3. Infinite Scroll
- Безкінечна горизонтальна анімація тексту
- Динамічне створення елементів до фіксованої ширини (2600px)
- Плавна анімація через `requestAnimationFrame`

### 4. Таблиця результатів
- Відображення топ-10 користувачів
- Підсвітка поточного користувача
- Показ сусідніх позицій для користувачів поза топ-10
- Перемикання між тижнями акції (1-4 тижні)

### 5. Dropdown функціональність
- Використання нативних `<details>` елементів
- Кастомна обробка scroll behavior при відкритті

---

## ⚠️ Виявлені проблеми

### Критичні помилки

1. **Невизначені змінні в `populateUsersTable()`:**
   ```javascript
   // Рядки 232-233: використовуються, але не визначені
   secondTableOther.innerHTML = '';
   secondTable.innerHTML = '';
   ```
   **Проблема:** Змінні `secondTable` та `secondTableOther` не оголошені, що призведе до `ReferenceError`.

2. **Невизначена змінна `isVerifiedUser`:**
   ```javascript
   // Рядки 222, 225, 228, 245: використовується, але не визначена
   if(userId && !currentUser && isVerifiedUser){
   ```
   **Проблема:** Змінна `isVerifiedUser` не оголошена, що призведе до `ReferenceError`.

### Проблеми в gulpfile.js

3. **Помилка в watch завданні:**
   ```javascript
   // Рядок 114: неправильний шлях
   gulp.watch('front/img/**/*.*', gulp.series('copyFonts'));
   // Має бути:
   gulp.watch('front/fonts/**/*.*', gulp.series('copyFonts'));
   ```

### Проблеми архітектури

4. **Абсолютні URL замість відносних:**
   - Зображення та API endpoints використовують hardcoded URL
   - Ускладнює локальну розробку та тестування

5. **Магічні значення:**
   - Числа без пояснень (2600px, 50ms, 20 спроб, тощо)
   - Краще винести в константи

6. **Закоментований код:**
   - Багато закоментованих блоків (рядки 14, 26, 136-144, 875-910)
   - Слід видалити або використати систему версіонування

7. **Відсутність оптимізації зображень:**
   - `gulp-imagemin` встановлений, але не використовується в gulpfile.js

---

## ✅ Сильні сторони

1. **Модульна структура:**
   - Чітке розділення на `front/` та `build/`
   - Організовані файли стилів та шаблонів

2. **BEM методологія:**
   - Консистентне іменування класів
   - Легко підтримувати та розширювати

3. **Сучасні технології:**
   - ES6+ синтаксис
   - Async/await для асинхронних операцій
   - Fetch API замість XMLHttpRequest

4. **Оптимізація продуктивності:**
   - `IntersectionObserver` для зупинки анімацій поза viewport
   - `requestAnimationFrame` для плавних анімацій
   - `gulp-newer` для оптимізації копіювання файлів

5. **Responsive Design:**
   - Mobile-first підхід
   - Адаптивні зображення через `<picture>`
   - Спеціальні медіа-запити для landscape орієнтації

6. **Обробка помилок:**
   - Try-catch блоки
   - Відправка помилок на сервер
   - Graceful degradation (редирект на stub сторінку)

---

## 📊 Статистика коду

### JavaScript (`front/js/main.js`)
- **Рядків коду:** ~913
- **Функцій:** ~15
- **Canvas анімацій:** 2 (олень, сніжинки)
- **API endpoints:** 2 (`/new-translates/${locale}`, `/api-cms/reports/add`)

### SASS файли
- `main.sass` - головний файл
- `var.sass` - змінні
- `def.sass` - дефолтні стилі
- `fonts.sass` - шрифти
- `media.sass` - медіа-запити
- `pages/index.sass` - стилі головної сторінки
- `pages/eng.sass` - стилі для англійської локалі
- `pages/dark.sass` - темна тема

### Pug шаблони
- `template.pug` - базовий шаблон
- `pages/BuildIndex.pug` - головна сторінка
- `tempPages/index.pug` - тимчасовий файл

---

## 🔍 Детальний аналіз компонентів

### 1. Ініціалізація (`init()`)
```javascript
// Послідовність ініціалізації:
loadTranslations() → init() → quickCheckAndRender()
```

**Особливості:**
- Polling для визначення `userId` (максимум 20 спроб, інтервал 50ms)
- Очікування `window.store` або `window.g_user_id`
- Автоматичний запуск анімацій після ініціалізації

### 2. Canvas анімація оленя
**Технічні деталі:**
- Розмір canvas: 1366×500px
- Спрайт-лист: 6 кадрів в рядку, 2 ряди
- Швидкість анімації: 8 кадрів між змінами
- Тривалість пасіння: ~3 секунди (180 кадрів при 60fps)

**Стани:**
- `WALKING` - рух між межами
- `GRAZING` - пасіння на краях
- `IDLE` - не використовується (зарезервовано)

### 3. Canvas анімація сніжинок
**Параметри:**
- Кількість: 50 сніжинок
- Розмір: 4-10px
- Швидкість: 0.1-0.3 px/frame
- Вітер: ±0.4px
- Інтервал створення: 200ms

**Особливості:**
- Адаптивний розмір canvas під `.fav-page`
- Автоматичне очищення при зміні розміру
- Зупинка анімації при виході з viewport

### 4. Infinite Scroll
**Алгоритм:**
1. Вимірювання ширини одного елемента
2. Створення елементів до досягнення 2600px
3. Клонування для безшовного циклу
4. Анімація зі швидкістю 0.3px/frame

---

## 🚀 Рекомендації для покращення

### Пріоритет 1 (Критичні)

1. **Виправити невизначені змінні:**
   ```javascript
   // Додати в початок файлу:
   const secondTable = document.querySelector("#secondTable");
   const secondTableOther = document.querySelector("#secondTableOther");
   let isVerifiedUser = false; // або отримати з API/state
   ```

2. **Виправити помилку в gulpfile.js:**
   ```javascript
   gulp.watch('front/fonts/**/*.*', gulp.series('copyFonts'));
   ```

### Пріоритет 2 (Важливі)

3. **Винести константи:**
   ```javascript
   const SCROLL_WIDTH = 2600;
   const SCROLL_SPEED = 0.3;
   const MAX_USER_ID_ATTEMPTS = 20;
   const USER_ID_CHECK_INTERVAL = 50;
   const SNOWFLAKE_COUNT = 50;
   ```

4. **Додати environment variables:**
   ```javascript
   const API_URL = process.env.API_URL || 'https://fav-prom.com/api_black_prize';
   ```

5. **Оптимізувати зображення:**
   ```javascript
   // В gulpfile.js додати в copyImg:
   .pipe(gulpIf(isProduction, imagemin()))
   ```

### Пріоритет 3 (Бажані)

6. **Видалити закоментований код** або додати TODO коментарі

7. **Додати TypeScript** для типізації

8. **Додати unit tests** для критичної функціональності

9. **Використати CSS Custom Properties** для динамічних значень

10. **Додати ESLint правила** для консистентності коду

---

## 📈 Метрики якості коду

| Метрика | Значення | Оцінка |
|---------|----------|--------|
| Модульність | Висока | ✅ |
| Читабельність | Середня | ⚠️ |
| Документація | Добра | ✅ |
| Обробка помилок | Добра | ✅ |
| Продуктивність | Висока | ✅ |
| Підтримуваність | Середня | ⚠️ |
| Тестування | Відсутнє | ❌ |

**Загальна оцінка: 7.5/10**

---

## 🎯 Висновки

### Сильні сторони:
- ✅ Добре структурований проєкт
- ✅ Використання сучасних технологій
- ✅ Оптимізація продуктивності
- ✅ Добре продуманий responsive design

### Слабкі сторони:
- ❌ Критичні помилки з невизначеними змінними
- ❌ Помилка в gulpfile.js
- ❌ Абсолютні URL замість відносних
- ❌ Відсутність тестів
- ❌ Закоментований код

### Рекомендації:
Проєкт потребує **негайного виправлення критичних помилок** перед деплоєм. Після виправлення помилок проєкт готовий до production, але буде виграти від рефакторингу та покращення підтримуваності.

---

**Дата аналізу:** 2025-01-27  
**Версія:** 1.0.0

